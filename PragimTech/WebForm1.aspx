<%@ Page Language="C#" AutoEventWireup="true" CodeBehind="WebForm1.aspx.cs" Inherits="PragimTech.WebForm1" %>

<!DOCTYPE html>

<html xmlns="http://www.w3.org/1999/xhtml">
<head runat="server">
    <title></title>
    <script type="text/javascript">

        ///filters

        // var myArray = [1,2,3,1,2,4,5,6,7,8,9,10];

        // myArray.filter(isEven);
        //   document.write(myArray.filter( function (value, index, array) {return array.indexOf(value) == index;})); // index returns first occurence of that element.

        /// Arrays

        //var myArray = new Array(3);
        //for(var i=0; i<5; i++)
        //{
        //    myArray[i] = new Array(5);
        //}

        //var start = 1;


        //for (var i=0; i<3;i++)
        //{
        //    for (var j=0;j<4;j++)
        //    {
        //        document.write(myArray[i][j] = start + "&emsp;");
        //        start = start + 1;
        //    }
        //    document.write("<br/>");
        //}


        //var factorial = function (fact) {
        //    if (fact <= 1)
        //        return 1;
        //    else return (fact * factorial(fact - 1))
        //}

        //document.write(factorial(5));

        //Closures in JS

        //function addNumbers(firstNumber, SecondNumber )
        //{
        //    var returnValue = "Result is : ";
        //    function add()
        //    {
        //        return returnValue +(firstNumber + SecondNumber);
        //    }
        //    return add; // if no parenthesis, it treats like an expression.
        //}

        ////var myFunc = addNumbers(10, 20);
        ////var result = myFunc();
        ////document.write(result);
        ////above three lines can be written in one line

        //document.write(addNumbers(10,20)());

        //arguments Objects

        //function someFunction()
        //{
        //    document.write(arguments[0], "&emsp;" , arguments[1]);
        //}

        //someFunction(10,20);



       </script>
</head>
<body>
</body>
</html>
